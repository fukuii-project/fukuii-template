# {{REPO_NAME}} — example environment file.
# Copy to `.env` and fill in real values. `.env` is gitignored; NEVER commit it.
# This file (`.env.example`) is committed and MUST contain only placeholders.

# --- example keys (delete/rename per repo) ---
# RPC_URL=https://rpc.example
# RPC_TOKEN=__set_me__
# DATABASE_URL=postgres://user:__password__@localhost:5432/db
# JWT_SECRET=__generate_a_random_secret__
# SIGNER_PRIVATE_KEY=__never_a_real_key_here__
